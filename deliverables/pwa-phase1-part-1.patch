From d2c39107c38b01ce63f34dfcac5538a5b9a620d9 Mon Sep 17 00:00:00 2001
From: Codex <codex@openai.com>
Date: Sun, 6 Sep 2026 05:51:58 +0000
Subject: [PATCH] fix: stabilize PWA navigation and resilient data loading

---
 .github/workflows/frontend-checks.yml |  16 +++
 index.html                            |  42 ++++----
 package-lock.json                     |  10 ++
 package.json                          |  12 +++
 pwa.js                                |  32 +++++-
 src/app/navigation-config.js          |  11 ++
 src/app/navigation.css                |  28 +++++
 src/app/navigation.js                 | 145 ++++++++++++++++++++++++++
 src/services/data-loader.js           |  31 ++++++
 src/services/resilient-cache.js       |  35 +++++++
 src/storage/legacy-storage.js         |  34 ++++++
 sw.js                                 |  76 ++++++--------
 tests/frontend/data-loader.test.js    |  31 ++++++
 tests/frontend/navigation.test.js     |  15 +++
 tests/frontend/service-worker.test.js |  15 +++
 tests/frontend/storage.test.js        |  42 ++++++++
 tools/build-check.js                  |  10 ++
 17 files changed, 520 insertions(+), 65 deletions(-)
 create mode 100644 .github/workflows/frontend-checks.yml
 create mode 100644 package-lock.json
 create mode 100644 package.json
 create mode 100644 src/app/navigation-config.js
 create mode 100644 src/app/navigation.css
 create mode 100644 src/app/navigation.js
 create mode 100644 src/services/data-loader.js
 create mode 100644 src/services/resilient-cache.js
 create mode 100644 src/storage/legacy-storage.js
 create mode 100644 tests/frontend/data-loader.test.js
 create mode 100644 tests/frontend/navigation.test.js
 create mode 100644 tests/frontend/service-worker.test.js
 create mode 100644 tests/frontend/storage.test.js
 create mode 100644 tools/build-check.js

diff --git a/.github/workflows/frontend-checks.yml b/.github/workflows/frontend-checks.yml
new file mode 100644
index 0000000..832cdde
--- /dev/null
+++ b/.github/workflows/frontend-checks.yml
@@ -0,0 +1,16 @@
+name: Frontend checks
+on:
+  pull_request:
+  push:
+    branches-ignore: [main]
+jobs:
+  check:
+    runs-on: ubuntu-latest
+    steps:
+      - uses: actions/checkout@v4
+      - uses: actions/setup-node@v4
+        with:
+          node-version: 22
+          cache: npm
+      - run: npm ci
+      - run: npm run check
diff --git a/index.html b/index.html
index ce07b0e..a7f24c1 100644
--- a/index.html
+++ b/index.html
@@ -4,7 +4,8 @@
     <script async src="https://pagead2.googlesyndication.com/pagead/js/adsbygoogle.js?client=ca-pub-7030081473208944"
      crossorigin="anonymous"></script>
     <meta charset="UTF-8">
-    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
+    <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover">
+    <link rel="stylesheet" href="./src/app/navigation.css">
     <title>台灣啦啦隊資料庫 | </title>
 
     <link rel="manifest" href="manifest.json">
@@ -1901,13 +1902,15 @@
         </div>
     </footer>
 
+    <script src="./src/storage/legacy-storage.js"></script>
+    <script src="./src/services/data-loader.js"></script>
     <script>
         // =========================================================================
         // 🚀 O(1) 極速快取字典與集合設定
         // =========================================================================
-        let myFavorites = JSON.parse(localStorage.getItem('cheer_favorites')) || [];
-        let mySchedules = JSON.parse(localStorage.getItem('cheer_my_schedules')) || [];
-        let myHomeTeam = localStorage.getItem('cheer_home_team') || "none";
+        let myFavorites = window.CheerStorage.readArray('cheer_favorites');
+        let mySchedules = window.CheerStorage.readArray('cheer_my_schedules');
+        let myHomeTeam = (() => { try { return localStorage.getItem('cheer_home_team') || "none"; } catch (_) { return "none"; } })();
 
         let favSet = new Set(myFavorites);
         let schedSet = new Set(mySchedules.map(s => s.id));
@@ -2054,9 +2057,8 @@
                 const nt = Date.now();
                 const newsBaseUrl = `https://docs.google.com/spreadsheets/d/e/2PACX-1vT9l-hRhzMwcRdyQHsRs_97fja0Gg4RCcDDMk31u-dSbbQmk_JIUmbPTAj2gaNYmb6bYTwUvv4_1IxN/pub?output=csv&rand=${nt}`;
                 const [freshManualNews, freshAutoNews] = await Promise.all([
-                    fetchCSV(`${newsBaseUrl}&gid=417186374&t=${nt}`).catch(() => []),
-                    fetch(`data/auto-news.json?t=${nt}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } })
-                        .then(r => r.ok ? r.json() : []).catch(() => [])
+                    window.CheerData.load('news-sheet', () => fetchCSV(`${newsBaseUrl}&gid=417186374&t=${nt}`)),
+                    window.CheerData.load('auto-news', () => fetch(`data/auto-news.json?t=${nt}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => { if (!r.ok) throw new Error(`auto-news HTTP ${r.status}`); return r.json(); }))
                 ]);
                 const seen = new Set();
                 const merged = [
@@ -2109,8 +2111,8 @@
                                 const nt = Date.now();
                                 const newsBaseUrl = `https://docs.google.com/spreadsheets/d/e/2PACX-1vT9l-hRhzMwcRdyQHsRs_97fja0Gg4RCcDDMk31u-dSbbQmk_JIUmbPTAj2gaNYmb6bYTwUvv4_1IxN/pub?output=csv&rand=${nt}`;
                                 const [freshManualNews, freshAutoNews] = await Promise.all([
-                                    fetchCSV(`${newsBaseUrl}&gid=417186374&t=${nt}`).catch(() => []),
-                                    fetch(`data/auto-news.json?t=${nt}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => r.ok ? r.json() : []).catch(() => [])
+                                    window.CheerData.load('news-sheet', () => fetchCSV(`${newsBaseUrl}&gid=417186374&t=${nt}`)),
+                                    window.CheerData.load('auto-news', () => fetch(`data/auto-news.json?t=${nt}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => { if (!r.ok) throw new Error(`auto-news HTTP ${r.status}`); return r.json(); }))
                                 ]);
 
                                 const newsSeen = new Set();
@@ -2134,9 +2136,8 @@
                                 const et = Date.now();
                                 const eventBaseUrl = `https://docs.google.com/spreadsheets/d/e/2PACX-1vT9l-hRhzMwcRdyQHsRs_97fja0Gg4RCcDDMk31u-dSbbQmk_JIUmbPTAj2gaNYmb6bYTwUvv4_1IxN/pub?output=csv&rand=${et}`;
                                 const [freshSheetEvents, freshManualEvents] = await Promise.all([
-                                    fetchCSV(`${eventBaseUrl}&gid=925411186&t=${et}`).catch(() => []),
-                                    fetch(`data/manual-events.json?t=${et}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } })
-                                        .then(r => r.ok ? r.json() : []).catch(() => [])
+                                    window.CheerData.load('events-sheet', () => fetchCSV(`${eventBaseUrl}&gid=925411186&t=${et}`)),
+                                    window.CheerData.load('manual-events', () => fetch(`data/manual-events.json?t=${et}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => { if (!r.ok) throw new Error(`manual-events HTTP ${r.status}`); return r.json(); }))
                                 ]);
                                 const eventSeen = new Set();
                                 dbEvents = [
@@ -2166,14 +2167,14 @@
 
             try {
                 const [girlsData, eventsData, schedulesData, newsData, matchesData, themesData, autoNewsData, manualEventsData] = await Promise.all([
-                    fetchCSV(`${baseUrl}&gid=0&t=${t}`).catch(() => []),
-                    fetchCSV(`${baseUrl}&gid=925411186&t=${t}`).catch(() => []),
-                    fetchCSV(`${baseUrl}&gid=1702657458&t=${t}`).catch(() => []),
-                    fetchCSV(`${baseUrl}&gid=417186374&t=${t}`).catch(() => []),
-                    fetchCSV(`${baseUrl}&gid=92509162&t=${t}`).catch(() => []),
-                    fetchCSV(`${baseUrl}&gid=547736461&t=${t}`).catch(() => []),
-                    fetch(`data/auto-news.json?t=${t}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => r.ok ? r.json() : []).catch(() => []),
-                    fetch(`data/manual-events.json?t=${t}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => r.ok ? r.json() : []).catch(() => [])
+                    window.CheerData.load('girls-sheet', () => fetchCSV(`${baseUrl}&gid=0&t=${t}`)),
+                    window.CheerData.load('events-sheet', () => fetchCSV(`${baseUrl}&gid=925411186&t=${t}`)),
+                    window.CheerData.load('schedules-sheet', () => fetchCSV(`${baseUrl}&gid=1702657458&t=${t}`)),
+                    window.CheerData.load('news-sheet', () => fetchCSV(`${baseUrl}&gid=417186374&t=${t}`)),
+                    window.CheerData.load('matches-sheet', () => fetchCSV(`${baseUrl}&gid=92509162&t=${t}`)),
+                    window.CheerData.load('themes-sheet', () => fetchCSV(`${baseUrl}&gid=547736461&t=${t}`)),
+                    window.CheerData.load('auto-news', () => fetch(`data/auto-news.json?t=${t}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => { if (!r.ok) throw new Error(`auto-news HTTP ${r.status}`); return r.json(); })),
+                    window.CheerData.load('manual-events', () => fetch(`data/manual-events.json?t=${t}`, { cache: 'no-store', headers: { 'Cache-Control': 'no-cache' } }).then(r => { if (!r.ok) throw new Error(`manual-events HTTP ${r.status}`); return r.json(); }))
                 ]);
 
                 dbGirls = girlsData.filter(i => i.realname); window.dbGirls = dbGirls;
@@ -8075,6 +8076,7 @@ window.renderMinigameStart = function() {
             if (currentMode === 'profile') window.closeProfile(); 
         };
     </script>
+    <script type="module" src="./src/app/navigation.js"></script>
 
 </body>
 </html>
diff --git a/package-lock.json b/package-lock.json
new file mode 100644
index 0000000..937e7ea
--- /dev/null
+++ b/package-lock.json
@@ -0,0 +1,10 @@
+{
+  "name": "twccheerleader",
+  "lockfileVersion": 3,
+  "requires": true,
+  "packages": {
+    "": {
+      "name": "twccheerleader"
+    }
+  }
+}
diff --git a/package.json b/package.json
new file mode 100644
index 0000000..5ff51f9
--- /dev/null
+++ b/package.json
@@ -0,0 +1,12 @@
+{
+  "name": "twccheerleader",
+  "private": true,
+  "type": "module",
+  "scripts": {
+    "syntax": "node --check src/app/navigation.js && node --check src/app/navigation-config.js && node --check src/services/resilient-cache.js && node --check src/services/data-loader.js && node --check src/storage/legacy-storage.js && node --check sw.js && node --check pwa.js",
+    "test": "node --test tests/frontend/*.test.js",
+    "build": "node tools/build-check.js",
+    "review": "node --experimental-vm-modules --test tests/review/pwa-foundation-regressions.mjs",
+    "check": "npm run syntax && npm test && npm run review && npm run build"
+  }
+}
diff --git a/pwa.js b/pwa.js
index e5e5c63..95690b3 100644
--- a/pwa.js
+++ b/pwa.js
@@ -1,13 +1,43 @@
 (() => {
   const isStandalone = () => window.matchMedia('(display-mode: standalone)').matches || window.navigator.standalone === true;
   let deferredPrompt = null;
+  let refreshing = false;
+  let updateAccepted = false;
 
   if ('serviceWorker' in navigator) {
     window.addEventListener('load', () => {
-      navigator.serviceWorker.register('./sw.js').catch(err => console.warn('Service worker registration failed:', err));
+      navigator.serviceWorker.register('./sw.js').then(registration => {
+        const offerUpdate = worker => {
+          if (!worker || !navigator.serviceWorker.controller || document.getElementById('pwa-update-btn')) return;
+          const btn = document.createElement('button');
+          btn.id = 'pwa-update-btn';
+          btn.type = 'button';
+          btn.textContent = '有新版本，點此更新';
+          btn.className = 'pwa-update-btn';
+          btn.onclick = () => {
+            updateAccepted = true;
+            btn.disabled = true;
+            worker.postMessage({ type: 'SKIP_WAITING' });
+          };
+          document.body.append(btn);
+        };
+        offerUpdate(registration.waiting);
+        registration.addEventListener('updatefound', () => {
+          const installing = registration.installing;
+          installing?.addEventListener('statechange', () => {
+            if (installing.state === 'installed') offerUpdate(registration.waiting || installing);
+          });
+        });
+      }).catch(err => console.warn('Service worker registration failed:', err));
     });
   }
 
+  navigator.serviceWorker?.addEventListener('controllerchange', () => {
+    if (refreshing || !updateAccepted) return;
+    refreshing = true;
+    location.reload();
+  });
+
   const createInstallButton = () => {
     if (document.getElementById('pwa-install-btn') || isStandalone()) return;
     const btn = document.createElement('button');
diff --git a/src/app/navigation-config.js b/src/app/navigation-config.js
new file mode 100644
index 0000000..deb243f
--- /dev/null
+++ b/src/app/navigation-config.js
@@ -0,0 +1,11 @@
+export const NAV_ITEMS = Object.freeze([
+  { id: 'girls', label: '女孩', mode: 'girls', icon: '<path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.9"/>' },
+  { id: 'events', label: '行程', mode: 'events', icon: '<rect x="3" y="5" width="18" height="16" rx="2"/><path d="M16 3v4M8 3v4M3 11h18"/>' },
+  { id: 'schedule', label: '班表', mode: 'schedule', modes: ['schedule', 'matches'], icon: '<path d="M4 4h16v16H4zM8 2v4M16 2v4M4 9h16M8 13h3M13 13h3M8 17h3"/>' },
+  { id: 'my', label: '我的', mode: 'my', modes: ['my', 'passport'], icon: '<path d="M20.8 4.6a5.5 5.5 0 0 0-7.8 0L12 5.7l-1.1-1.1a5.5 5.5 0 0 0-7.8 7.8L12 21l8.8-8.6a5.5 5.5 0 0 0 0-7.8z"/>' },
+  { id: 'more', label: '更多', mode: 'more', modes: ['more', 'news', 'games', 'vote', 'minigame', 'dreamteam', 'agency', 'feedback', 'themes', 'allstar'], icon: '<circle cx="5" cy="12" r="1"/><circle cx="12" cy="12" r="1"/><circle cx="19" cy="12" r="1"/>' }
+]);
+
+export function parentForMode(mode) {
+  return NAV_ITEMS.find(item => (item.modes || [item.mode]).includes(mode))?.id || 'more';
+}
diff --git a/src/app/navigation.css b/src/app/navigation.css
new file mode 100644
index 0000000..ce82a94
--- /dev/null
+++ b/src/app/navigation.css
@@ -0,0 +1,28 @@
+:root { --mobile-nav-height: 64px; }
+.primary-nav { position: sticky; top: 0; z-index: 9000; display: flex; justify-content: center; gap: 8px; padding: 8px 16px; background: rgba(10,12,15,.96); border-bottom: 1px solid #333; }
+.primary-nav__item { min-width: 88px; min-height: 48px; display: flex; align-items: center; justify-content: center; gap: 7px; border-radius: 10px; color: #aaa; font-weight: 800; text-decoration: none; }