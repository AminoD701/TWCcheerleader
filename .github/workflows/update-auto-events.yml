name: Update automatic public events

on:
  schedule:
    - cron: '15 22 * * *'
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'tools/fetch_auto_events.py'
      - 'tools/fetch_apify_social_events.py'
      - 'tools/dedupe_auto_events.py'
      - 'data/event-social-sources.json'
      - 'data/event-social-posts.json'
      - '.github/workflows/update-auto-events.yml'

permissions:
  contents: write

jobs:
  update-events:
    runs-on: ubuntu-latest
    env:
      APIFY_TOKEN: ${{ secrets.APIFY_TOKEN }}
    steps:
      - uses: actions/checkout@v4
      - name: Verify Apify token
        run: |
          if [ -z "$APIFY_TOKEN" ]; then
            echo "APIFY_TOKEN is not configured. Aborting without changing event data."
            exit 1
          fi
      - name: Install event URL decoder
        run: pip install googlenewsdecoder
      - name: Fetch supplemental public events
        run: python tools/fetch_auto_events.py
      - name: Fetch Instagram and Threads events via Apify
        run: python tools/fetch_apify_social_events.py
      - name: Deduplicate merged public events
        run: python tools/dedupe_auto_events.py
      - name: Commit updated events
        run: |
          git add data/auto-events.json
          if git diff --cached --quiet; then
            echo "No event changes"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Auto update public cheerleader events"
          git push
# retrigger after APIFY_TOKEN setup
